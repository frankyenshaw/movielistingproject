<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.frankyenshaw.movielistingproject.Activities.MainActivity">

    <Button
        android:text="Now Playing Movies"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/now_playing_button" />

    <Button
        android:text="Upcoming Movies"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/upcoming_button"
        android:layout_above="@+id/now_playing_button"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Popular Movies"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/popular_button"
        android:layout_below="@+id/now_playing_button"
        android:layout_alignLeft="@+id/upcoming_button"
        android:layout_alignStart="@+id/upcoming_button" />

</RelativeLayout>
